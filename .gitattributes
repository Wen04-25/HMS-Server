# Auto-detect text files and normalize line endings to LF in repo
* text=auto

# Ensure JavaScript, JSON, HTML, CSS use LF
*.js   text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css  text eol=lf
*.md   text eol=lf

# Windows-specific files can stay CRLF if needed
*.bat  text eol=crlf
*.cmd  text eol=crlf
